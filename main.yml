 - hosts: all
    tasks:
      - name: install the latest version of apache
        yum: 
          name: httpd 
          state: latest
      - name: start apache if not started
        service: 
          name: httpd 
          state: started
          ebabled: yes
